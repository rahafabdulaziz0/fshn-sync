# FSHN SYNC

Computer vision fashion search demo that finds visually similar clothing items from local Egyptian fashion stores.

Built with Python, Streamlit, CLIP embeddings, and Nearest Neighbors similarity search.

Run locally:
pip install -r requirements.txt
python -m streamlit run app/app/streamlit_app.py --server.port 8502
